# src/requirements.txt (수정 버전 - 의존성 충돌 해결 시도)

# Web Framework
uvicorn[standard]==0.30.1 # FastAPI와 함께 사용되는 ASGI 서버
fastapi==0.111.0

# AI APIs
openai==1.30.1
google-generativeai==0.4.0
elevenlabs==1.0.1

# NewsAPI
newsapi-python==0.2.7

# Video & Audio
pexels_api==1.0.1
moviepy==1.0.3
imageio==2.34.1
imageio-ffmpeg==0.4.9
ffmpeg-python==0.2.0

# Google Cloud 관련 라이브러리 - 핵심 종속성 버전 완화
google-cloud-storage==2.16.0
google-api-python-client==2.126.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-cloud-logging==3.8.0
google-cloud-aiplatform # 최신 버전 사용. 만약 충돌 시 특정 버전으로 고정 (예: google-cloud-aiplatform==1.30.0)
google-auth-httplib2==0.1.1
# protobuf는 Google 라이브러리의 핵심 종속성이므로, 버전을 완전히 명시하지 않아 pip가 가장 적합한 버전을 찾도록 합니다.
# 만약 충돌이 계속되면, protobuf>=3.19,<5.0.0 (혹은 더 넓은 범위)과 같이 최소/최대 버전을 지정하는 것을 고려합니다.

# Utils
requests==2.32.2
# Pydantic은 FastAPI의 핵심 종속성입니다. Pydantic v1 (1.x.x)과 v2 (2.x.x)는 호환되지 않습니다.
# FastAPI 0.111.0은 Pydantic v2와 호환되지만, 다른 하위 라이브러리가 Pydantic v1을 요구할 수 있습니다.
# 일단은 버전을 완화하여 pip가 최적 버전을 찾도록 시도합니다.
pydantic # pydantic==2.7.1 에서 버전을 완전히 제거하여 pip가 호환 버전을 찾도록 합니다.
pillow==11.3.0

# dotenv for local dev
python-dotenv==1.0.1

    # src/requirements.txt

    # 핵심 라이브러리
    google-cloud-secret-manager==2.24.0
    openai==1.35.13 # OpenAI 라이브러리
    google-cloud-storage==3.1.1
    google-generativeai==0.8.5 # Google Gemini 라이브러리
    elevenlabs==1.2.0 # ElevenLabs 음성 합성 라이브러리

    # Google 관련
    google-auth==2.29.0
    google-auth-oauthlib==1.2.0
    google-api-python-client==2.136.0

    # Cloud Functions
    functions-framework==3.* # Cloud Functions 개발 프레임워크

    # 미디어 처리 (중요: Cloud Function에서 moviepy와 ffmpeg는 성능/호환성 문제 가능성)
    # CPU/메모리 사용량이 높고, FFmpeg 바이너리 경로 설정 등 고려사항이 많습니다.
    # 만약 계속 문제가 발생하면, 이 부분을 Cloud Run이나 Compute Engine으로 분리하는 것을 고려해야 합니다.
    Pillow==10.3.0
    moviepy==1.0.3
    imageio-ffmpeg==0.4.9 # moviepy가 FFmpeg를 사용하도록 돕는 라이브러리

    # 기타 유틸리티
    newsapi-python==0.2.7 # 뉴스 API
    requests==2.32.3
    beautifulsoup4==4.12.3
    python-dotenv==1.0.1
    python-json-logger==2.0.7
    setuptools==68.0.0

    # 추가 설치
    Pexels==0.0.11 # Pexels API를 사용하기 위해 주석을 해제합니다.
    numpy==1.26.4
    pandas==2.2.2
    httplib2==0.22.0
    pydub==0.25.1 # pydub 라이브러리에 버전을 명시하여 안정성을 높입니다.
    

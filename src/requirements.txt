# src/requirements.txt

# Cloud Function 핵심 및 웹 프레임워크
functions-framework==3.4.0 # Cloud Function 진입점 및 HTTP 트리거 핸들링
Flask==3.0.2 # HTTP 요청 및 응답 처리를 위한 Flask (functions-framework가 내부적으로 사용)

# Google Cloud 서비스 연동
google-cloud-secret-manager==2.24.0 # GCP Secret Manager 접근
google-cloud-storage==2.16.0 # GCP Cloud Storage 접근
google-generativeai==0.8.5 # Google Gemini API 클라이언트
google-api-python-client==2.136.0 # YouTube Data API v3 (youtube_uploader.py에서 사용)

# Google 인증 관련
google-auth==2.29.0 # Google API 인증
google-auth-oauthlib==1.2.0 # OAuth2 인증 지원 (YouTube Data API 인증 흐름에 필요)

# AI 및 미디어 처리
openai==1.35.13 # OpenAI API 클라이언트 (GPT-4o 사용)
elevenlabs==1.2.0 # ElevenLabs TTS API 클라이언트
moviepy==1.0.3 # 동영상 편집 (FFmpeg 필요)
imageio-ffmpeg==0.4.9 # MoviePy가 FFmpeg 바이너리와 통신하기 위한 인터페이스
Pillow==10.3.0 # 이미지 처리 (MoviePy 등에서 내부적으로 사용될 수 있음)
pydub==0.25.1 # 오디오 처리 (FFmpeg 필요, TTS 결과물 처리 등에 사용될 수 있음)

# 기타 API 및 유틸리티
newsapi-python==0.2.7 # NewsAPI 클라이언트 (AI 콘텐츠 생성 소스로 활용 가능)
requests==2.32.3 # HTTP 요청 (Pexels API 직접 호출 등 범용적으로 사용)
python-slugify==8.0.4 # URL 친화적인 슬러그 생성 (선택 사항, 필요시 유지)

# 아래 라이브러리들은 현재 프로젝트에서 직접적인 사용처가 불분명하거나,
# 다른 라이브러리에 의해 이미 포함될 수 있어 제거합니다.
# flask-pymongo # MongoDB와 Flask 연동 (현재 코드에 MongoDB 사용 없음)
# beautifulsoup4 # 웹 스크래핑 (현재 코드에 웹 스크래핑 로직 없음, requests로 충분)
# python-dotenv # 로컬 개발용 (.env 파일 로드). Cloud Function에서는 환경 변수/Secrets Manager 사용
# python-json-logger # 로깅 형식 지정. Cloud Functions는 기본적으로 JSON 로깅을 지원
# numpy # MoviePy의 일부 기능에서 사용되지만, 명시적 버전 고정으로 충분할 수 있음.
# pandas # 데이터 분석 라이브러리 (현재 코드에 데이터 분석 로직 없음)
# setuptools # pip 설치 시 자동으로 처리되는 경우가 많음
